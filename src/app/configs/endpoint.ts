// Nothing serious here, so I just kept the endpoint as a constant.
//
// This makes both Vercel's Instant Deploy and local development easy.
// When running locally, run.py injects http://localhost:8000 as the backend endpoint

const API_ENDPOINT = "/api";

export default API_ENDPOINT;
